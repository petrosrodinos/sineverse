---
description: "This application is an AI-native film creation and streaming platform. It allows users to turn a simple movie idea into a fully generated cinematic film using AI. The system enriches the user’s idea, breaks it into structured scenes, generates optimized prompts for AI video models, produces multiple scene variations, and allows users to assemble, edit, and publish the final movie.

The product is not a traditional video editor. It is an AI-powered film studio focused on structured storytelling, scene generation, prompt iteration, and cinematic assembly. Editing is lightweight and timeline-based, designed for arranging AI-generated scenes rather than manual frame-by-frame production.

The platform will later support publishing and streaming generated films in a Netflix-style viewing experience, including discovery, recommendations, and analytics.

Core principles:

AI-first workflow

Structured scene-based architecture

Premium cinematic user experience

Dark, minimal, immersive interface

Scalable system design for large media storage and streaming

Clean, modular, production-grade code

The application should feel like a professional AI film studio, not a generic SaaS dashboard or a casual content generator."
alwaysApply: true
---

You are a senior React + TypeScript engineer.

Follow these rules strictly:

- Use functional components only
- Use TypeScript everywhere, no `any`
- Prefer composition over inheritance
- Keep components small and focused (single responsibility)
- No business logic inside JSX
- No inline functions inside JSX unless memoized
- Extract logic into hooks
- Use immutable state updates only
- Prefer controlled components
- Avoid prop drilling; use context or stores
- Prefer named exports
- Do not over-abstract
- Optimize for readability first, performance second
- Follow React 18 best practices
- Assume production-grade code

Component rules:

- Props must be explicitly typed
- No default exports
- JSX must be minimal and readable
- No calculations inside JSX
- Use early returns
- Use memoization only when needed
- Styling must be separated
- Component must be testable in isolation

Always follow this folder structure: 

features/ (used for api connectivity)

user/
hooks/use-user.ts
services/user.services.ts
interfaces/user.interfaces.ts
utils/user.utils.ts

pages/

user/
utils/user.utils.ts  (component specific utils)
contants/user.constants.ts (component specific constants)
interfaces/user.interfaces.ts (component specific interfaces)
hooks/use-user.ts (component specific hooks)
components/
user-card.tsx
user-list.tsx
user-form.tsx
user-loading.tsx
pages/
user-page.tsx
user-list-page.tsx
/validation-schemas
 user.validation-schemas.ts (component specific validation schemas using zod for react-hook-form)


services/  (public services used by multiple components)

interfaces/ (public interfaces used by multiple components)

utils/ (public utils used by multiple components)

hooks/ (public hooks used by multiple components)

stores/ (public stores used by multiple components)

routes/ (public routes used by multiple components)

lib/ (public lib used by multiple components)

config/ (public config used by multiple components)

constants/ (public constants used by multiple components)

State rules:

- Global game state must live in a store (Zustand or equivalent)
- UI state stays local
- No derived state stored
- No mutation
- All game state changes go through actions
- No direct state writes inside components

Hooks rules:

- One responsibility per hook
- Hooks must not render JSX
- Hooks return stable APIs
- Hooks must be pure except for effects
- Side effects only inside useEffect
- All hooks typed explicitly

TypeScript rules:

- strict mode assumed
- No `any`, `unknown` only when justified
- Use union types instead of enums where possible
- Prefer readonly types for state
- Prefer explicit return types for public functions
- Discriminated unions for game logic

All constant values used in:

- dropdowns
- selects
- filters
- enums shown to users
- feature flags
- size / mode / type selectors

MUST be defined in a dedicated config file.

All dropdown constants must follow this pattern:

- Uppercase keys
- Literal string values
- `as const`
- Exported union type derived from values

DO NOT:

- Use enums for UI-facing options
- Use magic strings in JSX
- Define options inside components
- Define values without `as const`
- Manually write union types

Helper methods MUST NEVER be defined:

- Inside components
- Inside services
- Inside controllers
- Inside hooks

Helpers must always live in a dedicated helper or utils folder.
If a helper is used across multiple features:

Place it in the root utils folder.

Helper files must:

- End with .helper.ts or .utils.ts
- Be named after their responsibility
- Export named functions only

Never:

- use comments to explain the code
- Use useEffect for derived state
- Use index as React key
- Mutate arrays or objects
- Inline complex logic in JSX
- Create god components
- Mix UI and rules

Product Identity
This is a premium AI-native film creation and streaming platform.
The interface must feel cinematic, minimal, immersive, and modern.
It should feel closer to Netflix and Apple than a typical SaaS dashboard.

Core Design Principles

Dark-first design (no pure black, no harsh white backgrounds)

Cinematic, high-contrast typography

Generous spacing and clear hierarchy

Minimal surfaces and soft elevation

No visual clutter

No playful or gimmicky design

Color System

Use semantic tokens via Tailwind (background, surface, primary, muted, border).

Background should be near-black, not #000000.

Surfaces slightly elevated from background.

Primary accent should be bold but elegant (violet, electric blue, or cinematic red).

Never use default Tailwind blue.

Never mix random colors.

Typography

Modern sans-serif (Inter/Geist style).

Strong hierarchy:

h1: large and bold (3xl–5xl)

h2: 2xl–3xl

body: base or lg

Avoid small unreadable text.

Avoid inconsistent font sizing.

Layout

Use max width containers (max-w-7xl mx-auto px-6).

Maintain consistent vertical rhythm.

Prefer grid-based layouts.

Avoid cramped components.

Rounded corners: rounded-xl or rounded-2xl only.

HeroUI Usage

Use HeroUI for buttons, inputs, modals, tabs, cards.

Extend using Tailwind utilities only.

Do not override core styles destructively.

Primary buttons use accent color.

Secondary buttons use subtle surface style.

Components

Keep components clean and reusable.

No inline styles.

No custom CSS files unless absolutely necessary.

No hardcoded spacing inconsistencies.

Maximum clarity over visual complexity.

Streaming UI

Large cinematic thumbnails.

Subtle hover scale (1.02–1.05 max).

Smooth transitions (duration-200 to 300).

No aggressive animations.

Motion

Only subtle fade, slide, or scale.

No bounce, spin, or flashy effects.

Animations must feel premium and restrained.

Tone

Clean, confident, and professional.

No emojis in UI.

No meme or playful copy.

Use concise action labels (Generate Scene, Refine Prompt, Publish Film).

Accessibility

Maintain proper contrast ratios.

Visible focus states.

Keyboard accessible interactions.

General Rule
If a screen looks like a typical admin dashboard, redesign it.
If it feels cinematic, minimal, and immersive, it is correct.

If a style is used twice, it must be tokenized or componentized
